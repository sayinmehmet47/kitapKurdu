# Legacy Kubernetes/Rancher Deployment Workflows

The files in this directory are **non-executable historical Kubernetes/Rancher deployment workflows**. They are retained for reference only.

The canonical deployment path is **Vercel (frontend) and Render (backend)**.

These workflow files must **not** be moved back into `.github/workflows/` or re-enabled without an explicit issue and review.
